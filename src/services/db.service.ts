/**
 * @deprecated Import directly from "../repositories" instead.
 * This file exists only for backwards compatibility while the codebase
 * migrates to the new repositories/ structure.
 */
export * from "../repositories";
export { default as db } from "../lib/prisma";
